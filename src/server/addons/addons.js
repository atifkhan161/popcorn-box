const Solar = require('./solar.moviez');
const yMovies = require('./ymovies');

const sources = [
  movieSources = [
    Solar(),
    yMovies()
  ],
  showSources = [
    Solar(),
    yMovies()
  ]
]

module.exports = sources;
